language: python

python:
  # - "3.5" # Doesn't work because of f-strings
  - "3.9.16"
  # No need to test for higher versions
  # - "3.7"
  # - "3.8"

install:
  - pip install --upgrade pip wheel setuptools
  - pip install --upgrade pytest coverage black coveralls
  - pip install .

before_script:
  - ls -lh
  - ls Pyramid-Scheme/ -lh

script:
  - bash test.sh -e -c 
  - black --check --verbose .

# Now done by github workflow
# after_success:
#   - coveralls